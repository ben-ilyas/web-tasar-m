html, body {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

.navbar {
    display: grid;
    grid-template-columns: 1fr auto 1fr; /* sol - logo - sağ */
    align-items: center;
    height: 80px;
    background: #f5f5f5;
    padding: 0 40px;
    border-bottom-left-radius: 30px;
    border-bottom-right-radius: 30px;
}

/* Logo tam ortada */
.navbar-logo {
    grid-column: 2; /* ortadaki sütun */
    display: flex;
    justify-content: center;
}

.navbar.shrink {
    height: 50px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.07);
}

.navbar-logo {
    grid-column: 2;
    display: flex;
    justify-content: center;
}

.navbar-logo img {
  height: 70px;
  transition: transform 0.3s ease; /* animasyon için */
  cursor: pointer; /* mouse pointer değişsin */
}

/* Hover olduğunda büyüme */
.navbar-logo img:hover {
  transform: scale(1.2); /* %20 büyüme */
}
.navbar-menu {
    display: flex;
    gap: 20px;
    list-style: none;
    margin: 0;
    padding: 0;
}
.navbar-menu.left {
    justify-content: flex-end;
}

/* Sağ menü sağa yaslanır */
.navbar-menu.right {
    justify-content: flex-start;
}


.navbar-menu li {
    margin: 0 10px; /* menü elemanları arasındaki boşluk biraz daha sıkı */
}

.navbar-menu li.active a {
  color: #fff;
  border-radius: 20px; /* köşeler yuvarlak */
  position: relative;
  z-index: 1;
  overflow: hidden;
}
.navbar-menu li.active a::before {
  content: "";
  position: absolute;
  top: 0; left: 0;
  width: 100%; height: 100%;
  background: #2eaf2c; /* aktif rengi */
  transform: scaleX(1); /* zaten dolu */
  transform-origin: left; /* başlangıç sol */
  transition: transform 0.3s ease;
  z-index: -1;
}

.navbar-menu li a {
  position: relative;
  display: inline-block;
  color: #222;
  text-decoration: none;
  font-weight: 600;
  padding: 10px 20px;
  border-radius: 20px; /* köşeler daha yuvarlak */
  overflow: hidden; /* animasyon dışarı taşmasın */
  transition: color 0.3s ease;
  z-index: 1;
}
.navbar-menu li a::before {
  content: "";
  position: absolute;
  top: 0; left: 0;
  width: 100%; height: 100%;
  background: #2eaf2c; /* hover rengi */
  transform: scaleX(0);
  transform-origin: left; /* default sol taraftan başlasın */
  transition: transform 0.3s ease;
  z-index: -1;
}
.navbar-menu li a::before {
  transform-origin: var(--origin, left);
}

/* Hover olunca animasyon */
.navbar-menu li a:hover::before {
  transform: scaleX(1);
}
.navbar-menu li a:hover {
  color: #fff;
}


.anasayfa-slider {
    position: relative;
    width: 100vw;
    height: calc(100vh - 110px); /* Navbar yüksekliğine göre ayarla */
    max-width: 100vw;
    max-height: 100vh;
    margin: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    box-sizing: border-box;
}

.slider-video {
    width: 100%;
    height: 100%;
    border-radius: 20px;
    margin-bottom: 20px;
    box-shadow: 0 4px 24px rgba(0,0,0,0.12);
    object-fit: cover;
    display: none; /* Video istersen göster, resim sliderı için gizli */
}

.slider-images {
    position: relative;
    width: 100vw;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
}

.slider-img {
    position: absolute;
    width: 100vw;
    height: 100%;
    object-fit: cover;
    border-radius: 0;
    opacity: 0;
    transition: opacity 0.5s;
    z-index: 1;
}

.slider-img.active {
    opacity: 1;
    z-index: 2;
}

.slider-arrow {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(255,255,255,0.6);
    border: none;
    border-radius: 50%;
    width: 48px;
    height: 48px;
    cursor: pointer;
    z-index: 3;
    opacity: 0.85;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    transition: background 0.2s;
    padding: 0;
}

.slider-arrow svg {
    display: block;
}

.slider-arrow:hover {
    background: rgba(255,255,255,0.9);
}

.slider-arrow.left {
    left: 40px;
}

.slider-arrow.right {
    right: 40px;
}

.anasayfa-slider:hover .slider-arrow {
    opacity: 1;
}

.mobile-only {
    display: none;
}
/* Mobil görünüm */
@media (max-width: 768px) {

     .desktop-only {
        display: none !important; /* sol ve sağ menüyü gizle */
    }
    .mobile-only {
        display: none;
        flex-direction: column;
        background: #f5f5f5;
        position: absolute;
        top: 80px;
        left: 0;
        width: 100%;
        padding: 20px 0;
        text-align: center;
        box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        z-index: 100;
    }
    .mobile-only.show {
        display: flex;
    }
    .navbar {
        grid-template-columns: auto auto; /* logo - hamburger */
        justify-content: space-between;
        padding: 0 20px;
    }

    /* Logo tam ortada */
    .navbar-logo {
        grid-column: 1 / -1; /* 2 sütunu kapla */
        justify-content: center;
    }

    /* Menüleri gizle */
    .navbar-menu {
        display: none;
        flex-direction: column;
        background: #f5f5f5;
        position: absolute;
        top: 80px;
        left: 0;
        width: 100%;
        padding: 20px 0;
        text-align: center;
        box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        z-index: 100;
    }

    /* Menü açıldığında */
    .navbar-menu.show {
        display: flex;
    }

    /* Hamburger */
    .hamburger {
        position: absolute;
        right: 20px;
        top: 25px;
        cursor: pointer;
        width: 30px;
        height: 22px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }

    .hamburger span {
        height: 4px;
        width: 100%;
        background: #222;
        border-radius: 2px;
    }
}
