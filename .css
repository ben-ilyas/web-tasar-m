html, body {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

.navbar {
    display: flex;
    align-items: center;
    background: #f5f5f5;
    padding: 0 40px;
    height: 80px;
    transition: height 0.3s, box-shadow 0.3s;
    border-bottom-left-radius: 30px;
    border-bottom-right-radius: 30px;
}

.navbar.shrink {
    height: 50px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.07);
}

.navbar-logo {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 0;
}

.navbar-logo img {
    height: 85px;
    margin-right: 30px;
}

.navbar-menu {
    flex: 2;
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
    justify-content: center;
}

.navbar-menu li {
    margin: 0 15px;
}

.navbar-menu li.active a {
    /* Aktif menü elemanı için normal stil */
    background: transparent;
    color: #222;
    border-radius: 4px;
    padding: 8px 16px;
    text-decoration: none;
}

.navbar-menu li a {
    color: #222;
    text-decoration: none;
    font-weight: 600;
    padding: 8px 16px;
    transition: background 0.2s, color 0.2s;
}

.navbar-menu li a:hover {
    background: #2eaf2c;
    color: #fff;
    border-radius: 4px;
    padding: 8px 16px;
    text-decoration: none;
}

.navbar-contact {
    flex: 1;
    display: flex;
    justify-content: flex-end;
    align-items: center;
    font-weight: bold;
    color: #222;
    margin-left: 30px;
    font-size: 1.1em;
}

.anasayfa-slider {
    position: relative;
    width: 100vw;
    height: calc(100vh - 110px); /* Navbar yüksekliğine göre ayarla */
    max-width: 100vw;
    max-height: 100vh;
    margin: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    box-sizing: border-box;
}

.slider-video {
    width: 100%;
    height: 100%;
    border-radius: 20px;
    margin-bottom: 20px;
    box-shadow: 0 4px 24px rgba(0,0,0,0.12);
    object-fit: cover;
    display: none; /* Video istersen göster, resim sliderı için gizli */
}

.slider-images {
    position: relative;
    width: 100vw;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
}

.slider-img {
    position: absolute;
    width: 100vw;
    height: 100%;
    object-fit: cover;
    border-radius: 0;
    opacity: 0;
    transition: opacity 0.5s;
    z-index: 1;
}

.slider-img.active {
    opacity: 1;
    z-index: 2;
}

.slider-arrow {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(255,255,255,0.6);
    border: none;
    border-radius: 50%;
    width: 48px;
    height: 48px;
    cursor: pointer;
    z-index: 3;
    opacity: 0.85;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    transition: background 0.2s;
    padding: 0;
}

.slider-arrow svg {
    display: block;
}

.slider-arrow:hover {
    background: rgba(255,255,255,0.9);
}

.slider-arrow.left {
    left: 40px;
}

.slider-arrow.right {
    right: 40px;
}

.anasayfa-slider:hover .slider-arrow {
    opacity: 1;
}
